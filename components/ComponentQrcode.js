import React from "react";
import { View } from "react-native";
import QRCode from "react-native-qrcode-svg";

const ComponentQrcode = ({ uuid }) => {
	return (
		<View>
			{/* <QRCode
				value={uuid}
				size={200}
				backgroundColor="white"
				color="black"
			/> */}
		</View>
	);
};

export default ComponentQrcode;
